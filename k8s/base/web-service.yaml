apiVersion: v1
kind: Service
metadata:
  name: servanda-web
  labels:
    app: servanda
    component: web
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
  selector:
    app: servanda
    component: web
