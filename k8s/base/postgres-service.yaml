apiVersion: v1
kind: Service
metadata:
  name: servanda-postgres
  labels:
    app: servanda
    component: postgres
spec:
  type: ClusterIP
  ports:
    - port: 5432
      targetPort: 5432
      protocol: TCP
  selector:
    app: servanda
    component: postgres
