apiVersion: v1
kind: Service
metadata:
  name: servanda-api
  labels:
    app: servanda
    component: api
spec:
  type: ClusterIP
  ports:
    - port: 3000
      targetPort: 3000
      protocol: TCP
  selector:
    app: servanda
    component: api
